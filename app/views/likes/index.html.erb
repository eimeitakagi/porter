<ul>
<% @notes.each do |note| %>
	<li style="border: 1px solid; margin: 10px; padding: 10px;">
		<div><%= link_to note.title, note_path(note.id) %></div>
		<div><%= note.content %></div>
		<div><%= note.like_count %>人がいいね！といっています</div>
		<div><%= note.created_at %></div>
	</li>
<% end %>
</ul>
